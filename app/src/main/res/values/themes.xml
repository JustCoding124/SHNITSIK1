<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- סגנון לשימוש ידני בכפתורים בלבד -->
    <style name="MyBlackButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">#000000</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- בסיס ערכת נושא (עם רקע לבן) -->
    <style name="Base.Theme.SHNITSIK" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- רקע לבן לאפליקציה -->
        <item name="android:windowBackground">#FFFFFF</item>
    </style>

    <!-- ערכת נושא ראשית -->
    <style name="Theme.SHNITSIK" parent="Base.Theme.SHNITSIK" />

</resources>
